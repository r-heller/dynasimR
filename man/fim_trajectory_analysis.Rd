% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fim_analysis.R
\name{fim_trajectory_analysis}
\alias{fim_trajectory_analysis}
\title{FIM trajectory analysis (REHASIM)}
\usage{
fim_trajectory_analysis(data, group_by = c("scenario"), longitudinal = FALSE)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or casualty/summary tibble
that contains at least \code{fim_admission} and \code{fim_discharge}.}

\item{group_by}{Character. Grouping columns. Default
\code{c("scenario")}.}

\item{longitudinal}{Logical. If \code{TRUE}, returns the per-timepoint
trajectories if a \code{fim_timeseries} slot is present in \code{data}.}
}
\value{
An S3 object of class \code{dynasimR_fim} with slots \code{summary},
\code{longitudinal} (if available) and \code{params}.
}
\description{
Summarises longitudinal Functional Independence Measure (FIM)
trajectories by scenario and (optionally) cohort. Returns both a
per-replication summary (admission, discharge, gain) and a
longitudinal tidy table suitable for \code{\link[=plot_fim_curves]{plot_fim_curves()}}.
}
