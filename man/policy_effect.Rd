% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy.R
\name{policy_effect}
\alias{policy_effect}
\title{Quantify policy effect (policy A vs. policy B)}
\usage{
policy_effect(
  data,
  policy_a_scenario,
  policy_b_scenario,
  alpha = 0.05,
  n_bootstrap = 1000
)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or summary tibble.}

\item{policy_a_scenario}{Character. Scenario ID for policy A.}

\item{policy_b_scenario}{Character. Scenario ID for policy B
(the comparator).}

\item{alpha}{Numeric. Significance level. Default \code{0.05}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
An S3 object of class \code{dynasimR_policy} with slots
\code{delta_event}, \code{compliance_comparison}, \code{effect_sizes},
\code{wilcoxon_tests}, \code{narrative}, and \code{params}.
}
\description{
Computes the full suite of effect metrics comparing two scenarios
that differ only in the allocation policy. Returns deltas on the
event rate and the Compliance Index, Cohen's d, risk differences
and an auto-generated narrative suitable for pasting into a
report.
}
\details{
No hardcoded defaults are used - you \strong{must} supply both scenario
IDs explicitly, so that the same function works for Profile A
(e.g. \code{"A-S08"} vs. \code{"A-S07"}) and Profile B (e.g. \code{"B-S19"} vs.
\code{"B-S00"}).
}
\examples{
\dontrun{
sim <- load_example_data()
pol <- policy_effect(sim,
  policy_a_scenario = "A-S08", policy_b_scenario = "A-S07")
print(pol)
cat(pol$narrative)
}
}
