% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ihl_compliance.R
\name{compute_ihl_index}
\alias{compute_ihl_index}
\title{IHL-Compliance Index}
\usage{
compute_ihl_index(
  casualties,
  window_min = 60,
  window_unit = c("min", "hours", "days"),
  by_scenario = TRUE,
  by_identity = TRUE,
  n_bootstrap = 1000
)
}
\arguments{
\item{casualties}{A \code{dynasimR_data} object, or a casualty-level
tibble/data.frame with a \code{time_to_first_care} column
(MEDTACS) or \code{waiting_days_to_min} column (REHASIM).}

\item{window_min}{Numeric. Window size, expressed in the unit given
by \code{window_unit}. Default \code{60}.}

\item{window_unit}{Character. One of \code{"min"}, \code{"hours"}, \code{"days"}.
Values are internally normalised to minutes.}

\item{by_scenario}{Logical. Compute per scenario. Default \code{TRUE}.}

\item{by_identity}{Logical. Stratify by \code{identity} if the column
is present. Default \code{TRUE}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
A tibble with columns \code{scenario} (if stratified),
\code{identity} (if stratified), \code{ici}, \code{ici_ci_lo}, \code{ici_ci_hi},
\code{n_total}, \code{n_treated_in_window}, \code{ihl_critical}, \code{window_min}.
}
\description{
Fraction of casualties (irrespective of identity) that receive
first care within a defined time window post-injury. A value
below \code{0.80} is treated as a critical IHL violation in the
MEDTACS-SIM ethics analysis.
}
\details{
For REHASIM data the analogous metric uses \code{waiting_days} with a
14-day window (\code{window_unit = "days"}).
}
\examples{
\dontrun{
sim <- load_example_data()
ihl <- compute_ihl_index(sim)
}
}
