% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compliance.R
\name{compute_compliance_index}
\alias{compute_compliance_index}
\title{Compliance Index}
\usage{
compute_compliance_index(
  entities,
  window_min = 60,
  window_unit = c("min", "hours", "days"),
  by_scenario = TRUE,
  by_group = TRUE,
  n_bootstrap = 1000
)
}
\arguments{
\item{entities}{A \code{dynasimR_data} object, or an entity-level
tibble/data.frame with a \code{time_to_first_service} column
(Profile A) or \code{wait_days_to_min} column (Profile B).}

\item{window_min}{Numeric. Window size, expressed in the unit
given by \code{window_unit}. Default \code{60}.}

\item{window_unit}{Character. One of \code{"min"}, \code{"hours"}, \code{"days"}.
Values are internally normalised to minutes.}

\item{by_scenario}{Logical. Compute per scenario. Default \code{TRUE}.}

\item{by_group}{Logical. Stratify by \code{group} if the column is
present. Default \code{TRUE}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
A tibble with columns \code{scenario} (if stratified),
\code{group} (if stratified), \code{ci}, \code{ci_ci_lo}, \code{ci_ci_hi},
\code{n_total}, \code{n_in_window}, \code{compliance_critical}, \code{window_min}.
}
\description{
Fraction of entities (irrespective of group) that receive service
within a defined time window. A value below \code{0.80} is treated as
a critical compliance violation.
}
\details{
For Profile B data the analogous metric uses \code{wait_days} with a
14-day window (\code{window_unit = "days"}).
}
\examples{
\dontrun{
sim <- load_example_data()
ci  <- compute_compliance_index(sim)
}
}
