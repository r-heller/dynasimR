% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autonomy.R
\name{al_efficiency}
\alias{al_efficiency}
\title{Compute AL-Efficiency ratio (event reduction vs. compliance)}
\usage{
al_efficiency(
  data,
  al_scenarios = c(`0` = "A-S00", `1` = "A-S05", `2` = "A-S09", `3` = "A-S10", `4` =
    "A-S11", `5` = "A-S12"),
  compliance_threshold = 0.8,
  reference_al = 0,
  n_bootstrap = 1000
)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or summary tibble.}

\item{al_scenarios}{Named character vector. Mapping AL level (as
a string) to scenario ID. Default assumes the Profile A
scenarios (with the "A-" prefix).}

\item{compliance_threshold}{Numeric. Minimum Compliance Index for
an AL level to be considered acceptable. Default \code{0.80}.}

\item{reference_al}{Integer. AL level used as reference for delta
calculation. Default \code{0}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
An S3 object of class \code{dynasimR_al_analysis} with slots
\code{tradeoff_table}, \code{optimal_al}, \code{compliance_violations} and
\code{params}.
}
\description{
Quantifies the trade-off between event-rate reduction and the
Compliance Index across autonomy levels AL0-AL5. Returns the
trade-off table, the optimal AL level (highest event reduction
still above the compliance threshold) and compliance violations.
}
\examples{
\dontrun{
sim <- load_example_data()
al  <- al_efficiency(sim)
plot_al_tradeoff(al)
}
}
