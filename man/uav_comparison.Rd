% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uav.R
\name{uav_comparison}
\alias{uav_comparison}
\title{Compare outcomes across UAV configurations}
\usage{
uav_comparison(
  data,
  scenarios = NULL,
  metrics = c("kia_rate", "ihl_compliance_index")
)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or summary tibble.}

\item{scenarios}{Character vector. Restrict to these IDs.
Default \code{NULL} = all.}

\item{metrics}{Character vector. Which metrics to report.
Default \code{c("kia_rate", "ihl_compliance_index")}.}
}
\value{
A tibble with one row per UAV configuration and columns
\code{uav}, \code{n_scenarios}, \code{n_reps}, and for each metric the median
and 2.5\%/97.5\% quantile.
}
\description{
Aggregates primary outcome metrics (KIA rate, IHL-Compliance Index
and — if present — median time-to-first-care) by the \code{uav} column
of the joined scenario metadata.
}
\examples{
\dontrun{
sim <- load_example_data()
uav_comparison(sim)
}
}
