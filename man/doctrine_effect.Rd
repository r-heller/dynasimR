% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doctrine.R
\name{doctrine_effect}
\alias{doctrine_effect}
\title{Quantify doctrine effect (MUF vs. Military Necessity)}
\usage{
doctrine_effect(
  data,
  muf_scenario,
  milnec_scenario,
  alpha = 0.05,
  n_bootstrap = 1000
)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or summary tibble.}

\item{muf_scenario}{Character. Scenario ID using MUF doctrine.}

\item{milnec_scenario}{Character. Scenario ID using Military
Necessity doctrine.}

\item{alpha}{Numeric. Significance level. Default \code{0.05}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
An S3 object of class \code{dynasimR_doctrine} with slots
\code{delta_kia}, \code{ihl_comparison}, \code{effect_sizes}, \code{wilcoxon_tests},
\code{narrative}, and \code{params}.
}
\description{
Computes the full suite of effect metrics comparing two scenarios
that differ only in medical doctrine (Medical Urgency First vs.
Military Necessity). Returns deltas on KIA rate and IHL-Compliance
Index, Cohen's d, risk differences and an auto-generated narrative
sentence suitable for pasting into a manuscript.
}
\details{
No hardcoded defaults are used — you \strong{must} supply both scenario
IDs explicitly, so that the same function works for MEDTACS
(e.g. \code{"M-S08"} vs. \code{"M-S07"}) and REHASIM (e.g. \code{"R-S19"} vs.
\code{"R-S00"}).
}
\examples{
\dontrun{
sim <- load_example_data()
doc <- doctrine_effect(sim,
  muf_scenario = "M-S08", milnec_scenario = "M-S07")
print(doc)
cat(doc$narrative)
}
}
