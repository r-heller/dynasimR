% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait_gap.R
\name{compute_wait_gap_index}
\alias{compute_wait_gap_index}
\title{Wait-Gap Index (Profile B)}
\usage{
compute_wait_gap_index(
  data,
  window_days = 14,
  by_scenario = TRUE,
  by_cohort = TRUE,
  n_bootstrap = 1000
)
}
\arguments{
\item{data}{A \code{dynasimR_data} object or entity tibble with a
\code{wait_days} column (or \code{wait_days_to_min} in minutes).}

\item{window_days}{Numeric. Allowed wait time in days.
Default \code{14}.}

\item{by_scenario}{Logical. Stratify by scenario. Default \code{TRUE}.}

\item{by_cohort}{Logical. Stratify by \code{cohort} (if present).
Default \code{TRUE}.}

\item{n_bootstrap}{Integer. Bootstrap replicates for CIs.
Default \code{1000}.}
}
\value{
A tibble with columns \code{scenario} (if stratified),
\code{cohort} (if stratified), \code{wgi}, \code{wgi_ci_lo}, \code{wgi_ci_hi},
\code{n_total}, \code{n_in_window}, \code{wgi_critical}, \code{window_days}.
}
\description{
Analogue of the Compliance Index for entity-flow profiles:
fraction of entities whose wait time from arrival to activation
does not exceed a threshold number of days (default 14). A value
below \code{0.80} is flagged as a critical wait-gap.
}
